namespace OPCAE.OPCAE
{
    public enum OPCAEFilters
    {
        OPC_FILTER_BY_EVENT = 1,
        OPC_FILTER_BY_CATEGORY = 2,
        OPC_FILTER_BY_SEVERITY = 4,
        OPC_FILTER_BY_AREA = 8,
        OPC_FILTER_BY_SOURCE = 0x10
    }
}

